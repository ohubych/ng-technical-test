/* Import { EExampleFeatureActions, ExampleFeatureActions } from '../actions/example-feature.actions';
   import { ExampleFeatureState, initialExampleFeatureState } from '../state/example-feature.state'; */

/* Export const ExampleFeatureReducers = (
       action: ExampleFeatureActions,
       state = initialExampleFeatureState
   ): ExampleFeatureState => {
       switch (action.type) {
           case EExampleFeatureActions.GetExampleFeatures: {
               return {
                   ...state,
                   example: 'example',
               };
           }
           default:
               return state;
       }
   }; */
